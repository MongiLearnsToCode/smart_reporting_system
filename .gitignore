.DS_Store
/node_modules/
.env
.env.local
.env*.local

# Next.js
/.next/
.vercel

# Convex generated API — committed so Vercel's `next build` resolves it (the
# `convex deploy --cmd` build runs next build before regenerating these).
# The ai-files guidance subfolder stays ignored.
/convex/_generated/ai/

# Local design inspiration, not part of the app
/UI_Inspo/
.superpowers/
