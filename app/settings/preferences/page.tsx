import { PreferencesPanel } from '@/components/settings/preferences-panel';

export const metadata = { title: 'Preferences | Codex' };

export default function PreferencesPage() {
  return <PreferencesPanel />;
}
